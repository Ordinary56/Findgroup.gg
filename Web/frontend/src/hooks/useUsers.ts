import { useEffect, useState } from "react";


const useUsers = (groupId: number) => {
    const [users, SetUsers] = useState();
    useEffect(() => {
        const fetchUsers = async () => {
            
        }
        fetchUsers();
    }, []);
};

export default useUsers;