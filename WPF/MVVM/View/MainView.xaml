<UserControl x:Class="WPF.MVVM.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.MVVM.View" xmlns:viewmodel="clr-namespace:WPF.MVVM.ViewModel" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700" Background="#b2b2b2">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="150" Width="300">


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Username:"/>
        <TextBox  Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Password:"/>
        <PasswordBox Grid.Column="1" Grid.Row="1"  x:Name="txtPassword" PasswordChar="*" KeyDown="txtPassword_KeyDown" />
        <Button Command="{Binding AuthenticateCommand}" Grid.Row="3" Grid.Column="1" Margin="10" Content="Login"/>
    </Grid>
</UserControl>
