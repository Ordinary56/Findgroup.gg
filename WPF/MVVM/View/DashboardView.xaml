<UserControl x:Class="WPF.MVVM.View.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.MVVM.View"
             mc:Ignorable="d" 
             Background="#2b2b2b"
             Height="800" Width="1000"
            >
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Viewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Sidebar with buttons -->
        <Border Grid.RowSpan="2" Background="#7c7c7c" Padding="10">
            <StackPanel>
                <TextBlock Text="Dashboard" Style="{StaticResource TextBlockStyle}" Margin="0 0 0 20"/>
                <Button Content="Export as CSV" Style="{StaticResource ButtonStyle}" Margin="0 20 0 10"/>
                <Button Content="Logout" Style="{StaticResource ButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- TabControl for the Lists -->
        <TabControl Grid.Column="1" Grid.Row="1" Margin="10">
            <!-- Users Tab -->
            <TabItem Header="Users" IsSelected="{Binding IsFirstListVisible}">
                <ListView VerticalAlignment="Stretch">
                    <ListView.View>
                        <GridView ColumnHeaderContainerStyle="{DynamicResource GridViewColumnHeaderStyle}">
                            <GridViewColumn Header="Users" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="17" Foreground="Salmon"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Actions" Width="180">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Edit" Width="80" Style="{StaticResource AlternateButtonStyle}" Command="{Binding EditCommand}"/>
                                            <Button Content="Delete" Width="80" Style="{StaticResource AlternateButtonStyle}" Command="{Binding DeleteCommand}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>

            <!-- Posts Tab -->
            <TabItem Header="Posts" IsSelected="{Binding IsSecondListVisible}">
                <ListView VerticalAlignment="Stretch">
                    <ListView.View>
                        <GridView ColumnHeaderContainerStyle="{DynamicResource GridViewColumnHeaderStyle}">
                            <GridViewColumn Header="Posts" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Description}" FontWeight="Bold" FontSize="17" Foreground="LightGreen"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Actions" Width="180">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Edit" Width="80" Style="{StaticResource AlternateButtonStyle}" Command="{Binding EditCommand}"/>
                                            <Button Content="Delete" Width="80" Style="{StaticResource AlternateButtonStyle}" Command="{Binding DeleteCommand}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>

            <!-- Groups Tab -->
            <TabItem Header="Groups" IsSelected="{Binding IsThirdListVisible}">
                <ListView VerticalAlignment="Stretch">
                    <ListView.View>
                        <GridView ColumnHeaderContainerStyle="{DynamicResource GridViewColumnHeaderStyle}">
                            <GridViewColumn Header="Groups" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Details}" FontWeight="Bold" FontSize="17" Foreground="LightBlue"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Actions" Width="180">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="Edit" Width="80" Style="{StaticResource AlternateButtonStyle}" Command="{Binding EditCommand}"/>
                                            <Button Content="Delete" Width="80" Style="{StaticResource AlternateButtonStyle}" Command="{Binding DeleteCommand}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
        </TabControl>
    </Grid>
    </Viewbox>
</UserControl>
